[target.aarch64-linux-android]
# 终极修复：使用 cross-rs 官方推荐的 GHCR 源，并使用最不容易出现 manifest unknown 错误的 ':latest' 标签。
# 之前的链接器问题已通过 Cargo.toml 修复，这应该能解决拉取问题。
image = "ghcr.io/cross-rs/aarch64-linux-android:latest"
